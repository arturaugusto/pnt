const Inspecao = Vue.component('inspecao', {
  props: ['desc', 'desc2'],
  template: document.getElementById('__template_Inspecao').innerHTML,
  data () {
    return {
    }
  },
  computed: {
  }
})